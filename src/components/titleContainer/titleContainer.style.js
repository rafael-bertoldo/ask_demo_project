import styled from "styled-components";

export const Container = styled.section`
  padding: 2rem 0;
  font-size: 1.5rem;

  > h3 {
    font-style: italic;
    font-weight: normal;
    font-size: 1.3rem;
    color: var(--dark-blue);
  }
`;
